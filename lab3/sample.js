export const lastIndexOf = (str, search) => str.lastIndexOf(search);
/**
 * Returns the last index of a search string within a given string
 * @param {string} str - The string to search within
 * @param {string} search - The string to search for
 * @returns {number} The last index of the search string, or -1 if not found
 */
export const replace = (str, search, replacement) => str.replace(search, replacement);
/**
 * Replaces occurrences of a search string with a replacement string
 * @param {string} str - The string to perform replacement on
 * @param {string} search - The string to search for
 * @param {string} replacement - The string to replace matches with
 * @returns {string} The string with replacements made
 */
export const split = (str, separator) => str.split(separator);
/**
 * Splits a string into an array of substrings based on a separator
 * @param {string} str - The string to split
 * @param {string} separator - The character or string to split on
 * @returns {string[]} An array of substrings
 */

